# ═══════════════════════════════════════════════════════════
# net2app SMS Hub — Python Backend Dependencies
# Python 3.10+ required, 3.11+ recommended
# Install: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════

# ── Web Framework ──
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
websockets==12.0

# ── Authentication ──
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Database (PostgreSQL) ──
sqlalchemy==2.0.25
asyncpg==0.29.0
alembic==1.13.1
psycopg2-binary==2.9.9

# ── SMPP Protocol ──
smpplib==2.2.1

# ── Redis (Cache / TPS / Sessions) ──
redis==5.0.1

# ── Message Queue (RabbitMQ) ──
pika==1.3.2
celery==5.3.6
kombu==5.3.4

# ── HTTP Client (Supplier API calls) ──
httpx==0.26.0
aiohttp==3.9.1

# ── Configuration ──
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0

# ── Validation ──
email-validator==2.1.0
phonenumbers==8.13.27

# ── Scheduling (Report gen, CDR cleanup) ──
apscheduler==3.10.4

# ── Logging & Monitoring ──
structlog==24.1.0
sentry-sdk==1.39.1

# ── Excel/CSV (Bulk rates, MCC/MNC, Campaigns) ──
openpyxl==3.1.2
pandas==2.1.4

# ── PDF Generation (Invoices) ──
reportlab==4.0.8

# ── Email (Notifications) ──
aiosmtplib==3.0.1
jinja2==3.1.3

# ── Testing ──
pytest==7.4.4
pytest-asyncio==0.23.3
